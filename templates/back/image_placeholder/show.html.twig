{% extends 'base.html.twig' %}

{% block title %}ImagePlaceholder{% endblock %}

{% block body %}
    <h1>ImagePlaceholder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image_placeholder.id }}</td>
            </tr>
            <tr>
                <th>ImgBase64</th>
                <td>{{ image_placeholder.imgBase64 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_image_placeholder_index') }}">back to list</a>

    <a href="{{ path('app_back_image_placeholder_edit', {'id': image_placeholder.id}) }}">edit</a>

    {{ include('back/image_placeholder/_delete_form.html.twig') }}
{% endblock %}
