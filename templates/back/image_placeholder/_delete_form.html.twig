<form method="post" action="{{ path('app_back_image_placeholder_delete', {'id': image_placeholder.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image_placeholder.id) }}">
    <button class="btn">Delete</button>
</form>
