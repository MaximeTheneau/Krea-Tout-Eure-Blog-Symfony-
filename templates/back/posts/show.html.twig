{% extends 'base.html.twig' %}

{% block title %}Posts{% endblock %}

{% block body %}
    <h1>Posts</h1>
    <img src={{ img.middle }} alt="">
    <table class="table">
        <tbody>
            <tr>
                <th>Title</th>
                <td>{{ post.title }}</td>
            </tr>
            <tr>
                <th>Sous Titre</th>
                <td>{{ post.subtitle }}</td>
            </tr>
            <tr>
                <th>Contents</th>
                <td>{{ post.contents }}</td>
            </tr>
            <tr>
                <th>Contents2</th>
                <td>{{ post.contents2 }}</td>
            </tr>
        </tbody>
    </table>
    
    <button class="button is-primary">{{ button_label|default('Sauvegarder') }}</button>

    <a href="{{ path('app_back_posts_index') }}" class="button is-light">Retours à la list</a>

    <a href="{{ path('app_back_posts_edit', {'id': post.id}) }}">Modifier</a>

    {{ include('back/posts/_delete_form.html.twig') }}
{% endblock %}
